interface PageHeroProps {
  title?: string;
  subtitle?: string;
  breadcrumb?: { label: string; path: string }[];
}

const PageHero = (_props: PageHeroProps) => {
  return null;
};

export default PageHero;
