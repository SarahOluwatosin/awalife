server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;

    # Never cache HTML (ensures new builds load immediately)
    location = /index.html {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
    }
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
    }

    # Cache static assets aggressively (Vite fingerprints them)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # News article detail pages — social crawlers get server-rendered OG HTML;
    # regular browsers continue to receive the React SPA.
    location ~ ^/company/news/[^/]+$ {
        if ($is_social_crawler = "1") {
            proxy_pass http://127.0.0.1:3001;
        }
        try_files $uri $uri/ /index.html;
    }

    # All routes fall back to index.html (React SPA routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
